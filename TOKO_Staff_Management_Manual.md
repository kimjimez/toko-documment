---
title: "TOKO — คู่มือการจัดการพนักงานในร้าน"
subtitle: "Merchant App: เพิ่ม / อนุมัติ / พักงาน / ให้พ้นจากการเป็นพนักงาน + ระบบบันทึกการเข้า-ออกงาน"
author: "TOKO Operations"
date: "2026-05-12"
version: "1.0"
---

# คู่มือการจัดการพนักงานในร้าน (Staff Management)

เอกสารนี้อธิบายขั้นตอนการใช้งานระบบจัดการพนักงานในแอป **Toko Merchant**
สำหรับเจ้าของร้าน (Owner) และพนักงาน (Staff) รวมถึงระบบบันทึกการเข้า-ออกงาน
(Clock-in / Clock-out) พร้อมการแจ้งเตือน (Notification + Email) และรายงาน
ที่บันทึกตำแหน่ง GPS ทุกครั้ง

---

## 1. ภาพรวมสถานะพนักงาน

| สถานะ (status) | ความหมาย | สิทธิ์ของพนักงาน | การกระทำของระบบ |
| --- | --- | --- | --- |
| `pending` | รออนุมัติ (สมัครจากประกาศรับสมัคร) | ยังไม่เห็นร้าน | รอ Owner อนุมัติ |
| `active` | พนักงานอยู่ | เห็นร้าน + เข้า-ออกงานได้ | ปกติ |
| `on_leave` | พักงาน | **ยังเห็นร้าน** แต่กดเข้างานไม่ได้ | บังคับ logout 1 ครั้ง |
| `inactive` | พ้นจากการเป็นพนักงาน | ไม่เห็นร้าน | Soft-delete record + บังคับ logout |
| `rejected` | ไม่อนุมัติคำสมัคร | ไม่เห็นร้าน | แจ้งผลทางอีเมล |

> ข้อมูลประวัติการเข้า-ออกงาน (`market_staff_attendances`) จะถูกเก็บไว้
> ตลอด แม้สถานะจะเปลี่ยนเป็น `on_leave` หรือ `inactive` ก็ตาม
> เพื่อใช้สำหรับรายงานย้อนหลังและการคำนวณค่าแรง

---

## 2. ขั้นตอนสำหรับเจ้าของร้าน (Owner)

### 2.1 เข้าสู่หน้าจัดการพนักงาน
1. เปิดแอป **Toko Merchant** เข้าสู่ระบบด้วยบัญชีเจ้าของร้าน
2. เลือกร้านที่ต้องการจัดการ (กรณีมีหลายร้าน)
3. เปิดเมนู **"การจัดการพนักงาน"** จากหน้าหลัก
4. หน้าจอจะแสดง:
   - การ์ดสรุป: จำนวนพนักงานทั้งหมด / กำลังทำงาน
   - กลุ่ม **"พนักงานที่ทำงาน"** (`status = active`)
   - กลุ่ม **"พนักงานที่ไม่ได้ทำงาน"** (status อื่น ๆ ที่ไม่ใช่ active)

### 2.2 เพิ่มพนักงานใหม่
1. กดปุ่ม **"+ เพิ่มพนักงาน"** มุมขวาล่าง
2. ค้นหาผู้ใช้จากเบอร์โทรหรืออีเมล
3. กรอกข้อมูล:
   - ตำแหน่ง (Position)
   - ค่าแรงต่อชั่วโมง (Hourly rate)
   - บทบาท: `staff` / `supervisor` / `manager`
4. กดบันทึก → ระบบสร้าง record ทันทีด้วย `status = active`

> **กรณีที่ผู้ใช้คนนี้เคยถูกให้พ้นจากการเป็นพนักงานของร้านนี้มาก่อน**
> ระบบจะทำการ **Restore** record เดิมโดยอัตโนมัติ → ประวัติการเข้า-ออกงาน
> ทั้งหมดยังอยู่ครบ ไม่หาย

### 2.3 อนุมัติ / ปฏิเสธคำสมัคร (Recruitment)
หากร้านเปิดประกาศรับสมัคร พนักงานสามารถสมัครจากแอป Super App ได้
1. คำสมัครที่เข้ามาจะปรากฏในกลุ่ม **"พนักงานที่ไม่ได้ทำงาน"** สถานะ `pending`
2. กดเข้าไปที่การ์ด → กดปุ่ม **อนุมัติ** หรือ **ปฏิเสธ**
3. ระบบจะส่งอีเมลแจ้งผลให้ผู้สมัครอัตโนมัติ

### 2.4 ปรับสถานะพนักงาน
จากหน้ารายละเอียดพนักงาน เลือก dropdown สถานะเป็น 1 ใน 3 ตัวเลือก:

| ตัวเลือก | ผลลัพธ์ |
| --- | --- |
| **พนักงานอยู่ (Active)** | กลับมาทำงานปกติ |
| **พักงาน** | พนักงานยังเห็นร้านแต่กดเข้างานไม่ได้ + ถูก logout 1 ครั้ง |
| **พ้นจากการเป็นพนักงาน** | Record ถูก soft-delete + พนักงานถูก logout + ไม่เห็นร้านอีก |

> เมื่อเปลี่ยนเป็น `on_leave` หรือ `inactive` ระบบจะ revoke `api_token` +
> `device_token` ของผู้ใช้ทันที **เฉพาะกรณีไม่มีร้านอื่นเหลือเป็น
> active หรือ owner** เพื่อบังคับให้แอปฝั่งพนักงาน sync สถานะใหม่

### 2.5 ดูรายงานการเข้า-ออกงานของพนักงานทั้งร้าน
1. จากหน้าหลัก เลื่อนการ์ดร้านลงมา → กด **"ดูรายงานพนักงานทั้งหมด"**
2. ระบบเปิดหน้ารายงาน:
   - แสดงป้าย **"มุมมองเจ้าของร้าน"** มุมขวาบน
   - **Default criteria คือวันนี้** (from = to = today)
   - สามารถเลือกช่วงวันที่ได้ผ่านปุ่มปฏิทิน 2 ปุ่ม
   - แสดงรายการเข้างาน + ออกงาน + ระยะเวลาทำงาน + ชื่อพนักงาน
   - คลิกไอคอน 🗺️ ที่แต่ละแถว เพื่อดูตำแหน่งบนแผนที่ (ในแอป)

---

## 3. ขั้นตอนสำหรับพนักงาน (Staff)

### 3.1 เข้าสู่ระบบ
- ใช้บัญชีปกติของแอป **Toko Merchant**
- หลังจากเจ้าของร้านอนุมัติ ร้านจะปรากฏเป็นแท็บใหม่บนหน้าหลัก
- ป้ายสีฟ้าใต้ชื่อร้านจะแสดงคำว่า **"พนักงาน"**

### 3.2 บันทึกเข้างาน (Clock-in)
1. ที่การ์ดร้าน เลื่อนลงไปท้ายการ์ด → กดปุ่ม **"เข้างาน (Clock In)"** สีเขียว
2. แอปจะขอสิทธิ์ตำแหน่ง (GPS) ครั้งแรก → อนุญาต
3. ระบบบันทึก:
   - เวลา (server time)
   - พิกัด lat/lng (ความแม่นยำสูง)
   - ที่อยู่โดยประมาณ (ถ้าระบุ)
4. ระบบส่ง:
   - **Notification + Email** ถึงตัวพนักงาน → "บันทึกเข้างานสำเร็จ"
   - **Notification + Email** ถึงเจ้าของร้านทุกคน → "พนักงานชื่อ X เข้างานเวลา ..."

### 3.3 บันทึกเลิกงาน (Clock-out)
1. ปุ่มจะเปลี่ยนเป็น **"ออกงาน (Clock Out)"** สีส้ม
2. กด → ยืนยันใน Dialog
3. ระบบบันทึกพิกัด + คำนวณระยะเวลาทำงาน (`work_duration_minutes`)
4. ระบบส่ง Notification + Email แจ้งผล (พร้อมระยะเวลาทำงาน) ให้พนักงาน
   และเจ้าของร้าน

> **GPS เป็นข้อบังคับ** — หากไม่อนุญาต / ปิด GPS ระบบจะ ไม่บันทึก ให้
> และแสดงข้อความ "กรุณาเปิด GPS เพื่อบันทึกตำแหน่ง"

### 3.4 ดูรายงานของตัวเอง
1. กดปุ่ม **"ดูรายงานการเข้า-ออกงานของฉัน"** บนการ์ดร้าน
2. หน้ารายงานจะแสดง **"มุมมองพนักงาน"** + รายการของตัวเองเท่านั้น
3. กดไอคอนแผนที่เพื่อตรวจสอบตำแหน่งที่เคยบันทึก

### 3.5 กรณีถูกพักงาน (`on_leave`)
- ร้านยังปรากฏในรายการ แต่การ์ดร้านจะแสดง banner สีส้ม
  **"สถานะ: พักงาน — ไม่สามารถบันทึกเข้า-ออกงานได้"**
- ปุ่มเข้างาน/ออกงาน **ถูกซ่อน**
- ปุ่ม "ดูรายงานของฉัน" ยังกดได้ (ตรวจสอบประวัติเก่า)
- เจ้าของร้านสามารถปรับกลับเป็น `active` เพื่อให้กลับมาทำงานได้ทันที

### 3.6 กรณีถูกให้พ้นจากการเป็นพนักงาน (`inactive`)
- ระบบ logout ทันทีในการเรียก API ครั้งถัดไป
- ครั้งหน้า login ใหม่จะ **ไม่เห็นร้านนี้** อีก
- หากเจ้าของร้านเลือก "เพิ่มพนักงาน" คนเดิมกลับเข้ามาในภายหลัง
  → record เดิมถูก restore + ประวัติเก่าทั้งหมดยังอยู่

---

## 4. การแจ้งเตือนและอีเมล (Notification + Email)

ทุกครั้งที่พนักงานบันทึกเข้างานหรือเลิกงาน ระบบจะส่ง 2 channel พร้อมกัน:

| ผู้รับ | Database (in-app) | Email |
| --- | --- | --- |
| ตัวพนักงานเอง | ✅ | ✅ (ถ้ามี email) |
| เจ้าของร้านทุกคนใน `user_markets` | ✅ | ✅ (ถ้ามี email) |

เนื้อหาอีเมลประกอบด้วย:
- ชื่อพนักงาน + ชื่อร้าน
- เวลา check-in / check-out
- พิกัด lat/lng
- ปุ่ม **"ดูตำแหน่งบนแผนที่"** ที่ลิงก์ไปยัง Google Maps
- (สำหรับ check-out) ระยะเวลาทำงานในรูปแบบ "X ชม. Y นาที"

---

## 5. ความปลอดภัยและข้อมูลที่บันทึก

ทุกแถวของ `market_staff_attendances` จะมี:

| Field | คำอธิบาย |
| --- | --- |
| `user_id` | ผู้บันทึก |
| `market_id` | ร้านที่บันทึก |
| `check_in_time`, `check_out_time` | เวลาที่ระบบ (server) |
| `check_in_latitude`, `check_in_longitude` | พิกัดตอนเข้างาน (decimal 10,7) |
| `check_in_address` | ที่อยู่โดยประมาณ (ถ้ามี) |
| `check_out_latitude`, `check_out_longitude` | พิกัดตอนเลิกงาน |
| `check_out_address` | ที่อยู่โดยประมาณ (ถ้ามี) |
| `work_duration_minutes` | คำนวณอัตโนมัติตอน check-out |
| `status` | `working` / `completed` / `incomplete` |
| `notes` | หมายเหตุเพิ่มเติม (ถ้ามี) |

### สิทธิ์การเข้าถึงรายงาน
| บทบาท | เห็นได้ |
| --- | --- |
| Owner / Admin | **ทุกแถวของร้าน** (ทุกพนักงาน) |
| Staff (active) | เฉพาะแถวของตัวเอง |
| Staff (on_leave) | เฉพาะแถวของตัวเอง (อ่านอย่างเดียว) |
| ผู้ใช้อื่น | 403 Forbidden |

ฝั่ง response มี `meta.is_owner_view: true|false` ให้แอปใช้แสดงป้าย
"มุมมองเจ้าของร้าน / มุมมองพนักงาน" ได้ถูกต้อง

---

## 6. Reference: API Endpoints

> ฐาน URL: `<base>/api`
> Auth: Bearer token ใน header `Authorization: Bearer <api_token>`

### 6.1 จัดการรายชื่อพนักงาน
| Method | Path | คำอธิบาย |
| --- | --- | --- |
| GET | `/markets/{market_id}/staff` | ดึงรายชื่อพนักงานในร้าน |
| POST | `/markets/{market_id}/staff` | เพิ่มพนักงานใหม่ (auto-restore ถ้ามี trashed) |
| POST | `/markets/{market_id}/staff/apply` | สมัครงานจากประกาศรับสมัคร |
| PUT | `/market-staff/{id}` | แก้ไขข้อมูล / เปลี่ยนสถานะ (active/inactive/on_leave) |
| POST | `/market-staff/{id}/application-status` | อนุมัติ / ปฏิเสธ (pending → active/rejected) |
| DELETE | `/market-staff/{id}` | ลบพนักงานออกจากร้าน (hard remove) |

### 6.2 บันทึกเข้า-ออกงาน
| Method | Path | คำอธิบาย |
| --- | --- | --- |
| POST | `/markets/{market_id}/attendance/check-in` | บันทึกเข้างาน (require lat/lng) |
| POST | `/markets/{market_id}/attendance/check-out` | บันทึกเลิกงาน (require lat/lng) |
| GET  | `/markets/{market_id}/attendance/me` | ดึง record `working` ปัจจุบันของผู้ใช้ |
| GET  | `/markets/{market_id}/attendances?from_date&to_date&status` | รายงาน (owner เห็นทุกคน, staff เห็นเฉพาะตน) |

### 6.3 ตัวอย่าง Payload — Check-in
```json
POST /api/markets/12/attendance/check-in
Authorization: Bearer xxx
Content-Type: application/json

{
  "latitude": 13.736717,
  "longitude": 100.523186,
  "address": "ถนนพระราม 4 แขวงสีลม เขตบางรัก",
  "notes": "เริ่มกะเช้า"
}
```

### 6.4 ตัวอย่าง Response — รายงาน
```json
{
  "success": true,
  "data": [
    {
      "id": 188,
      "user_id": 47,
      "market_id": 12,
      "staff_name": "ณิชาภา สองเมือง",
      "market_name": "ร้านอุดม Seafood",
      "check_in_time": "2026-05-12 11:14:23",
      "check_in_latitude": 13.736717,
      "check_in_longitude": 100.523186,
      "check_out_time": "2026-05-12 11:16:05",
      "check_out_latitude": 13.736701,
      "check_out_longitude": 100.523199,
      "work_duration_minutes": 1,
      "status": "completed"
    }
  ],
  "meta": {
    "is_owner_view": true,
    "total": 1
  }
}
```

---

## 7. Troubleshooting

| อาการ | สาเหตุที่เป็นไปได้ | วิธีแก้ |
| --- | --- | --- |
| กดเข้างานแล้วขึ้น "กรุณาเปิด GPS" | GPS ปิดบนเครื่อง | เปิด Location Service / อนุญาตสิทธิ์ |
| กดเข้างานแล้วขึ้น "ไม่ได้รับสิทธิ์เข้าถึงตำแหน่ง" | Permission denied (forever) | ไปตั้งค่า → แอป → อนุญาตตำแหน่ง |
| Owner กดพ้นจากการเป็นพนักงานแล้วยังเห็นในรายชื่อ | แอปยัง cache อยู่ | กดปุ่มรีเฟรช 🔄 มุมขวาบนของหน้าจัดการพนักงาน |
| พนักงานยังเห็นร้านหลังถูกพ้น | ยังไม่ได้เรียก API ใหม่หลัง force-logout | ปิดแอปแล้วเปิดใหม่ / login อีกครั้ง |
| รายงานไม่มีข้อมูลทั้งที่เคยบันทึก | criteria ไม่ครอบคลุมวัน | เคลียร์ตัวกรองวันที่ (กดกากบาท) |
| Email ไม่เข้า | ผู้ใช้ไม่มี email หรือคิวเมล blocked | ตรวจสอบที่ตั้งค่า user + log `failed_jobs` |

---

## 8. Changelog

| Version | Date | Note |
| --- | --- | --- |
| 1.0 | 2026-05-12 | เอกสารแรก ครอบคลุมการจัดการพนักงาน + ระบบบันทึกการเข้า-ออกงาน + auto-restore เมื่อกลับมาเป็นพนักงาน |
